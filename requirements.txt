# Raspberry Pi deployments should use distro packages:
#   sudo apt install python3-pyqt6 python3-opencv python3-numpy
# Expose them to a development venv with:
#   python3 -m venv --system-site-packages .venv

# Pinned direct runtime dependencies for wheel-based environments.
PyQt6==6.7.1
PyQt6-sip==13.8.0
numpy==1.26.4

# PyPI's OpenCV wheel lacks GStreamer, so the app falls back to V4L2.
# Use a distro or custom OpenCV build with GStreamer enabled for that backend.
opencv-python==4.10.0.84

# Development test dependencies.
pytest==8.3.2
pytest-qt==4.4.0
